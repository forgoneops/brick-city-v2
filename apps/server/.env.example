# MySQL connection string (matches docker-compose.yml for local dev)
DATABASE_URL=mysql://bcm:bcm@localhost:3306/brickcity

# Secret used to sign JWT access tokens — change in production
JWT_SECRET=change-me-in-production

# Port for the Hono HTTP server
PORT=8787

# CORS origin for the Vite dev server
WEB_ORIGIN=http://localhost:5173
